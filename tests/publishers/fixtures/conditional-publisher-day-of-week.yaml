publishers:
    - conditional-publisher:
        - condition-kind: day-of-week
          day-selector: weekday
          use-build-time: true
          action:
              - archive:
                  artifacts: '**/**'
                  allow-empty: 'true'
